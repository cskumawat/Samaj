# Project Memory

- Samaj must always be created, installed, tested, and run inside the project `.venv`.
- Never install dependencies globally and do not use `pip --user` for this project.
- Anti-Hallucination Mode remains ON: do not claim unsupported tools are implemented.
- The user requested a comprehensive phase-wise Tool Catalog / Knowledge Registry that may exceed Kali Linux. The intake is stored in `samaj/tools/catalog_manifest.json` and documented in `docs/tool_catalog_roadmap.md`.
- Every tool entry is neutral catalog intake only. Do not claim entries are installed, supported, tested, safe, recommended, licensed, endorsed, executable, permitted, or built by Samaj.
- Do not block catalog entry creation only because a tool is offensive-capable, lab-only, unknown, or AI-suggested; require disclaimer acceptance before import/detail viewing instead.
- `TOOL_CATALOG_DISCLAIMER.md` acceptance is mandatory before catalog import, detail viewing, or catalog use.
- Phase 2 scope is foundation only: database and CRUD, neutral catalog intake, OS support mapping, disclaimer gate, static-only Safe Analysis Lab, proxy database, and theme/logo foundation.
- Tool Catalog entry does not mean tool support, endorsement, verification, safety, installation, execution, recommendation, operational support, or permission to use.
- Safe Analysis Lab does not execute malware on the host and must not be treated as a complete malware sandbox until dynamic VM isolation is separately implemented and tested.
- Proxy features must not be used for illegal activity, abuse, bypassing bans, bypassing rate limits, unauthorized scraping, credential attacks, or hiding unauthorized activity.
